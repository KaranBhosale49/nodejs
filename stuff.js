var counter=function(arr){
    return "there are "+ arr.length+" in this array" ;
 }

 var adder=function(a,b){
    return `addition of to no is ${a+b}`;
 }

 var pi=3.142

 module.exports = {
    counter:counter,
    adder:adder,
    pi:pi
 };